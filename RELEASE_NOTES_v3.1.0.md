# AdbFileManager v3.1.0 发布说明

v3.1.0 是从 v2.6.1 升级而来的大型功能版本，重点是双栏文件管理、可靠的 Windows Unicode 文件传输、代码编辑器、交互式终端和更完整的设备工具。

## 重点更新

### 双栏文件管理器

- 左侧为本地电脑，右侧为 Android 设备，支持方向箭头双向复制。
- 本地栏聚焦浏览和传输，不提供删除、重命名、权限修改等破坏性操作。
- 支持本地栏折叠，并记住用户选择，兼顾旧版单栏使用习惯。
- 复制或移动到本地成功后自动刷新左栏。
- 支持多选、全选、批量导出、批量移动和批量删除。

### Windows 中文及 Unicode 路径修复

- 所有 ADB 命令改用参数数组交给 `ProcessBuilder`，不再通过空格拆分拼接后的命令字符串。
- Windows 上传采用纯 ASCII 临时文件名，再通过 Android Shell 重命名为原始 UTF-8 文件名。
- 修复 GBK/CP936 系统中以下类型文件名上传失败或损坏的问题：
  - 含空格的 ASCII 文件名，例如 `trust you.docx`；
  - 中文及中文加空格文件名；
  - 日文或其他当前 ANSI 代码页无法表示的字符。
- 导出前验证本地目标目录是否可写；受保护目录会显示清晰提示并禁用相关操作。

### 拖放与传输

- 支持本地与 Android 两栏之间双向拖放，以及与系统文件管理器之间拖放。
- 拖放改为从独立手柄发起，解决点击进入文件夹时容易误触复制的问题。
- 传输对话框的取消和关闭现在会真正取消任务并终止 ADB 子进程。
- 取消后会立即复位顶部进度状态，并清理由本次传输创建的残缺文件和远程暂存文件。
- 已经存在的同名文件不会因为取消操作而被误删。

### 内置代码编辑器

- 支持常见文本和代码文件的语法高亮。
- 支持查找、替换、全部替换、撤销和重做。
- 显示编码、光标行列、字符数和行数。
- 支持重新选择文本编码，并修复中文保存后变成问号的问题。
- 对原编码无法表示的新内容自动使用无损 UTF-8 保存。

### 交互式终端

- 新增多标签终端，可执行 ADB 命令和设备 Shell 命令。
- 支持命令历史、清屏、停止当前命令和终端模式切换。
- 支持将本地文件拖入终端并自动追加带引号的路径。

### 设备与文件工具

- 新增应用列表、APK 备份和卸载。
- 新增截图及剪贴板传输入口。
- 完善书签、搜索、排序、权限修改、重命名、复制和移动。
- 改进文件类型图标、空目录状态和操作反馈。

### 桌面体验与稳定性

- 初始窗口尺寸根据显示器工作区动态计算并自动居中。
- 支持浅色、深色、跟随系统主题以及中英文界面。
- 修复空列表边界可能触发的 `SnapshotStateList.removeLast()` 异常。
- ADB 运行时启动前逐项比较 SHA-256 和文件大小；内容一致时不再覆盖。
- ADB 版本变化时先解压到临时目录再替换；若 Windows 正在占用 `adb.exe`，会延后更新并继续使用完整的现有运行时。
- 增加运行时安装锁，避免多个应用实例并发解压。

## 与 v2.6.1 的主要区别

| 功能 | v2.6.1 | v3.1.0 |
| --- | --- | --- |
| 文件布局 | Android 单栏 | 本地 + Android 双栏，可折叠 |
| Windows 空格/Unicode 上传 | 可能失败或损坏文件名 | 参数化执行 + ASCII 暂存 + UTF-8 重命名 |
| 双向复制 | 依赖导入/导出对话框 | 方向箭头、批量操作、双向拖放 |
| 传输取消 | 仅关闭对话框 | 终止任务和 ADB 进程并清理残缺文件 |
| 文本编辑 | 基础编辑 | 语法高亮、查找替换、撤销重做、编码保护 |
| 终端 | 无完整交互终端 | 多标签 ADB/Shell 终端 |
| 应用工具 | 基础文件管理 | 应用列表、APK 备份/卸载、截图、剪贴板 |
| 窗口布局 | 固定初始尺寸 | 根据屏幕工作区动态计算 |
| ADB 运行时 | 启动时可能重复覆盖 | SHA-256 校验、临时更新、占用保护 |

## 安装与升级

Windows 用户运行 `AdbFileManager-win-3.1.0.msi` 完成安装。升级不会修改 Android 设备上的文件；建议退出正在运行的旧版应用后再安装。

首次运行前请确保 Android 设备已经开启 USB 调试，并在设备上接受电脑的 ADB 调试授权。

## 已知说明

- Android 端实际可访问范围仍取决于设备版本、厂商系统和 ADB Shell 权限。
- `C:\Users`、`C:\Windows` 等 Windows 受保护目录通常不可直接写入，请选择自己的用户目录或其他可写目录作为导出目标。
- 远程目录中已经由旧版或其他工具以错误字节创建的乱码名称，不会自动重命名。

## 校验

- 版本标签：`v3.1.0`
- Windows 包版本：`3.1.0`
- 安装包：`AdbFileManager-win-3.1.0.msi`（118,546,180 字节 / 113.05 MiB）
- SHA-256：`7C3B42E0D01205833F74ABFBA4900B27EDF48B708F284B46873D43A43CB5150E`
- 数字签名：未签名；Windows 可能显示“未知发布者”，请使用以上 SHA-256 校验下载文件
- 完整测试：通过
